<div id="menu">
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <ul class="nav navbar-nav">
        <li><%= link_to "home", {controller: "/page", action: "home"} do %><%= t('.home') %><% end %></li>
      </ul>
      <% if current_user.try(:admin)%>
      <ul class="nav navbar-nav">
        <li><%= link_to("users") do %><%= t('.users') %><% end %></li>
      </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% unless user_signed_in? %>
        <li><%= link_to("/register") do %><span class="glyphicon glyphicon-user"></span> <%= t('.sign_up') %><% end %></li>
        <li><%= link_to("/login") do %><span class="glyphicon glyphicon-log-in"></span> <%= t('.login') %><% end %></li>
        <% else %>
        <li><%= link_to "note_add", "/new_site", class: "material-icons" %></li>
        <li><a href="/profile/<%= current_user.id %>">
          <!--<% if current_user.img_url %>
          <div>
            <img width="24" height="24" src="/images/<%= current_user.img_url %>" class="round">
          </div>
          <% else %>
          <img width="24" height="24" avatar="<%= current_user.name %>" class="round">
          <% end %>-->
          <img width="24" height="24" avatar="<%= current_user.name %>" class="round">
        </a></li>
        <li><a href="/exit" class="material-icons">exit_to_app</a></li>
        <% end %>
      </ul>
    </div>
  </nav>
</div>